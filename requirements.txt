# CryptoAgent — Phase 1 dependencies (Python 3.11+)
# All free-tier; no paid API keys required.
ccxt>=4.2,<5
pandas>=2.1,<3
numpy>=1.26,<2
ta>=0.11,<0.12
requests>=2.31,<3
chromadb>=0.5,<0.6
python-dotenv>=1.0,<2
# sqlite3 is part of the Python standard library — not listed here.
